<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ncheck.mapper.SysUserMapper">

    <select id="selectByUsername" resultType="com.ncheck.entity.SysUser">
        SELECT id, username, password_hash, nickname, avatar_color, role, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

</mapper>
