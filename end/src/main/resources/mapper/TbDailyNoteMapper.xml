<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ncheck.mapper.TbDailyNoteMapper">

    <select id="selectByUserAndDate" resultType="com.ncheck.entity.TbDailyNote">
        SELECT id, user_id, review_date, note_content, create_time, update_time
        FROM tb_daily_note
        WHERE user_id = #{userId} AND review_date = #{reviewDate}
        LIMIT 1
    </select>

</mapper>
